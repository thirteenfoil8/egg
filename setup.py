from setuptools import setup, find_packages

setup(
    name='egg',
    version='0.1.0',
    description='Egg Project',
    author='Random swiss guy',
    author_email='example@example.com',
    url='https://uselessmachine.ch',
    packages=find_packages(include=['egg'])
)
